<?php
  require_once "src/Anagram.php";

  class AnagramTest extends PHPUnit_Framework_TestCase
  {
      function test_Anagram()
      {
          //Arrange
          $test_Anagram = new Anagram;
          $input = "nap";

          //Act
          $result = $test_Anagram->makeAnagram($input_word, $input_list);
          
          //Assert
          $this->assertEquals("nap", $result);
      }
  }

 ?>



 <!-- if a user enters the word "nap" and a related list of words: "pan" "anp" "pna" "apn", the output will be pan -->
